version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: optima-app:latest
    container_name: optima_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
